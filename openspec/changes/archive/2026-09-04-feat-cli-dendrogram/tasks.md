## 1. Contratos y validación (rojo primero)

- [x] 1.1 `tests/test_cli.py` flags --method / --covariance-estimator / --linkage(-method) / --save/--show: `_build_parser().parse_args(["--method","hrp",...])` asserts `weight_allocation_method`, `covariance_estimator`, `linkage_method`, `save`/`show` defaults + `choices` invalido → `SystemExit` 2, y propagation con `monkeypatch` capturando `generate_complete_analysis_report` `config`+`provider`+`save_plots`/`show_plots`. Verificar: `pytest tests/test_cli.py -k flag` rojo pre-impl (AttributeError dest inexistente).
- [x] 1.2 `tests/test_dendrogram.py` headless: `plot_hrp_dendrogram` sobre `cov` 3x3 y 12-asset synthetic block → PNG existe `>1KB`, `plt.get_backend()=="agg"` bajo `MPLBACKEND` ausente, orden hojas == `_leaf_order` == `leaves_list(Z)`, `n=1`/`n=2` no crash (warning), `n=0` no-op. Verificar: `ImportError` plot_hrp_dendrogram rojo pre-impl.

## 2. Implementación core

- [x] 2.1 `portfolio/portfolio/hrp.py` seam `build_hrp_linkage(cov, linkage_method="single") -> np.ndarray` (valida `LINKAGE_METHODS` pre-scipy, guards square/finite/sym/diag>0 idénticos a `calculate_hrp_weights:69-76`), extraer distancia firmada `sqrt(max(0.5*(1 - _correlations_from_cov(cov)),0))`, `squareform`+`linkage`. Refactor `calculate_hrp_weights` para usar seam (snapshot single bit-identical). Exportar via `__init__.py`. Verificar: `test_hrp` single snapshot intacto (sin modificar asserts).
- [x] 2.2 `portfolio/viz/reporting.py` `plot_hrp_dendrogram(cov, linkage_method, tickers, save_path, show_plot)` con `scipy.cluster.hierarchy.dendrogram`, `labels=tickers`, `leaf_rotation=90`, `figsize` escalado, `color_threshold=None`, `tight_layout`, `_finalize_plot`. Guard `n<2` (warning). Usar `colormaps[...].with_extremes` si colormap custom (no `set_bad` sobre compartido). Verificar: headless PNG vía `tmp_path`.
- [x] 2.3 `portfolio/cli.py` `_build_parser` añade `--method` (dest `weight_allocation_method`, choices 6), `--covariance-estimator` (choices 3), `--linkage`+`--linkage-method` (dest `linkage_method`, choices 3, default single), `--save`/`--no-save` y `--show`/`--no-show` (`BooleanOptionalAction`, defaults True/False), `--universe` metavar PATH, `--refresh-cache`. `main(argv, universe_path)` construye `PortfolioConfig(method, cov_estimator, linkage_method, ...)` desde `args`, provider con `refresh_cache`, y `generate_complete_analysis_report(..., save_plots=args.save, show_plots=args.show, provider=provider)` manteniendo legacy `universe_path` branch. Verificar: `--help` contiene `--method`/`--covariance-estimator`/`--linkage`/`--save`/`--show`/`--refresh-cache`.
- [x] 2.4 `portfolio/app/pipeline.py` añade `CHART_FILENAMES["hrp_dendrogram"]="hrp_dendrogram.png"`, tras `optimal_portfolio_analysis` invoca `plot_hrp_dendrogram(covariance_matrix, config.linkage_method, list(filtered_metrics.keys()), save_path if save_plots else None, show_plot)` en `try/except` warning, actualiza log `plots=8`, no amplía tupla retorno. Verificar: `test_pipeline_e2e` con provider sintético genera 8º archivo sin `ValueError` sobre `M>=2`.

## 3. Integración y cierre

- [x] 3.1 `portfolio_engine/__init__.py` exporta `plot_hrp_dendrogram` y `build_hrp_linkage` + `__all__` + docstring 7→8. Verificar: `from portfolio_engine import plot_hrp_dendrogram, build_hrp_linkage` ok.
- [x] 3.2 Suite completa `./init.sh` exit 0: `216→~230` passed (15+ nuevos), `All checks passed!`, `pyright 0 errors`, `compileall OK`, headless warnings 0 (`PendingDeprecationWarning` set_bad no introducido). Verificar.
- [x] 3.3 Specs `openspec validate --specs` (package-interface + runtime-diagnostics) + `CHANGELOG.md` Unreleased (Added CLI flags + dendrogram, Changed pipeline 7→8) + `README.md:77` tabla 8. Verificar.
